# (주)케이원 홈페이지 — 인수인계 문서

> 이 파일은 **다른 계정/다른 세션에서 이 프로젝트를 이어받기 위한 인수인계서**입니다.
> 세영님(하오웹 실무 담당)과 함께 만든 케이원 홈페이지의 현황·원칙·작업방식을 정리했습니다.
> 작성 기준일: 2026-08-11 · 최신 커밋 `bf40db9`

---

## 0. 가장 먼저 — 새 계정에서 이어받는 법

1. 저장소를 받는다: `git clone https://github.com/seyeong1403/K1-homepage.git`
   (또는 이 폴더 `C:\Users\PC\Desktop\k1-homepage` 를 그대로 복사)
2. **이 문서(`_인수인계.md`)를 먼저 끝까지 읽는다.**
3. 아래 **§5 작업 원칙**을 반드시 지킨다. 세영님이 여러 번 강조한 것들이라 어기면 재작업이 난다.
4. 소통은 **전부 한글**로 한다(최종 답변뿐 아니라 중간 설명도).

---

## 1. 프로젝트 개요

- **무엇**: (주)케이원(K1 Co., Ltd.) 기업 홈페이지. 해양플랜트·조선·에너지 산업의 **통합 시운전(Turnkey Commissioning) 전문기업**.
- **자회사**: 케이원오션(K1 OCEAN) — Marine / 선박 시운전.
- **성격**: 아직 **디자인·수정 단계**(오픈 전). 업체(클라이언트) 확인 후 문서·수정 요청이 들어오면 반영하는 흐름.
- **콘텐츠 출처**: Figma 원본 + 업체가 주는 수정사항 문서(.docx/.pptx). **텍스트·수치·구성은 원본/업체 지시 기준**, 임의 창작 금지(§5 참조).

---

## 2. 기술 스택 · 파일 구조

- **정적 사이트**. 프레임워크 없음(바닐라 HTML/CSS/JS). 빌드 과정 없음 — 파일을 그대로 GitHub Pages가 서빙.
- **페이지 7개**(모두 같은 헤더/푸터/메가메뉴 셸 공유):
  `index.html`(메인) · `about.html`(회사소개) · `business.html`(사업분야) · `projects.html`(프로젝트 실적) · `recruit.html`(인재채용) · `news.html`(홍보센터) · `contact.html`(고객문의)
- **공용 자산**:
  - `css/v2.css` — **전 페이지 공용 단일 스타일시트**. 여기만 고치면 모든 페이지에 반영됨.
  - `js/v2.js` — 공용 스크립트(헤더 적응형·메가메뉴 hover-intent·모바일 드로어·카운트업 등).
  - `assets/` — 로고(`logo-k1*.png`, `mark-k1*.png`), 히어로(`hero.jpg`), 사업분야 사진(`biz-0N-*.jpg`), 프로젝트 사진(`pj-*.jpg`), 아이콘(`assets/icons/`, CSS mask 방식), 고객사 로고(`assets/logos/`).
- ~~삭제 예정: `mockup-a/b/c.html` (초기 시안 목업)~~ → **2026-08-31 삭제 완료**. 필요 시 `git show b581ec0:mockup-a.html` 로 복구.
- **작업 자료 폴더**(사이트 파일 아님, 업체 제공 원본 보관): `메인페이지 이미지/`, `이미지 추가/`, `주요고객사 로고/`, `_archive/`(구 버전 백업).
- **로컬 미리보기**: `.claude/launch.json` 에 정적 서버 등록되어 있을 수 있으나, **PS 정적서버 + 이미지 많은 페이지는 프리뷰 스크린샷 도구와 자주 충돌**함(§6 참조).

---

## 3. 배포 (GitHub Pages)

- **저장소**: `https://github.com/seyeong1403/K1-homepage.git` (origin, `main` 브랜치)
- **라이브 URL(임시 도메인)**: **https://seyeong1403.github.io/K1-homepage/**
- **배포 방식**: `main` 에 push 하면 GitHub Pages가 자동 반영(1~2분). 별도 빌드 없음.
- **커밋 관행**: 작업 한 건이 끝나면 커밋·푸시. 커밋 메시지 끝에 다음 줄 포함:
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
  - ⚠️ `force push`·히스토리 변경·새 저장소 생성 등 되돌리기 어려운 작업은 **세영님께 먼저 확인**.
  - 취소·되돌리기는 파일 삭제보다 `git revert`(이력 보존)를 쓴다 — 나중에 복구 가능.

---

## 4. 디자인 시스템 — "Signal at Sea"

`css/v2.css` 최상단 `:root` 에 토큰 정의. 주요 값:

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--bg0` | `#081521` | 다크 섹션/히어로 딥네이비 |
| `--paper` / `--paper2` | `#F4F1EB` / `#EBE6DC` | 밝은 섹션 웜 오프화이트 |
| `--ink` / `--ink-mut` | `#101922` / `#5A6976` | 본문 / 보조 텍스트 |
| **`--orange`** | **`#008BBC`** | **포인트 컬러 = K1 블루** (아래 ⚠️) |
| `--orange-2` | `#23A4D6` | 밝은 블루 |
| `--orange-ink` | `#04668C` | 진한 블루(버튼 hover 등) |
| `--wrap` | `1560px` | 콘텐츠 최대폭 |
| `--pad` | `clamp(22px,5.2vw,96px)` | 좌우 여백 |

> ### ⚠️⚠️ 매우 중요 — 포인트 컬러 함정
> 원래 포인트 컬러는 **오렌지(#E1571F)** 였으나, 클라이언트 요청으로 **K1 로고색과 비슷한 파란색으로 전면 교체**했습니다.
> **단, CSS 변수 이름은 `--orange` 그대로 두고 값만 블루로 바꿨습니다**(전 파일의 `var(--orange)` 를 일일이 안 고치려고).
> 즉 코드에 `--orange` 라고 써 있어도 **실제 색은 파란색(#008BBC)** 입니다. 헷갈리지 말 것.
> 새로 색을 넣을 때도 이 블루 토큰(`var(--orange)` 계열)을 쓰면 됩니다.

- **폰트**: **Pretendard 단일**(jsdelivr CDN, SIL OFL 라이선스 — 상업적 사용 안전). 본문 `word-break:keep-all`.
- **로고**: K1 / K1 OCEAN 엠블럼(파란 삼각 + "Korea No.1"). 헤더에서 배경에 따라 2종 전환(`logo-k1.png` / `logo-k1-dark.png`).

---

## 5. ⭐ 작업 원칙 (세영님과 정한 것 — 반드시 준수)

세영님이 반복해서 강조했거나 지적받아 확립된 규칙들입니다.

### 5-1. 콘텐츠
- **텍스트·수치·구성은 원본(Figma)/업체 지시 기준.** 문구를 임의로 창작·의역·재배치하지 않는다.
- 섹션 종류·순서·레이아웃도 함부로 추가/변경하지 않는다(과거 구성을 바꿔 지적받은 이력 있음).
- 업체 수정요청 문서(.docx/.pptx)가 오면 **지시 그대로** 반영한다. 애매하면 넘겨짚지 말고 **질문**한다.
- 없는 정보(수상, 실적 수치 등)를 지어내지 않는다. 데이터 없으면 정직한 빈 상태로.

### 5-2. 디자인 표기
- **본문 `word-break:keep-all`** — 한글이 단어 중간에서 끊기지 않게(모든 웹 제작 디폴트).
- **부수 텍스트(네비·라벨·캡션)는 조금 더 크게.** 단, 타이틀·소제목·큰 숫자는 유지.
- **버튼·카드는 모서리 라운드**(radius 토큰 사용). "카드 지양" 지침은 **철회됨** — 카드 정상 사용.
- 폰트·크기는 기존 토큰 유지. 레퍼런스의 px 값을 그대로 옮기지 말 것.

### 5-3. 정렬·품질 (중요)
- **작업 끝마다 정렬을 좌표로 검증한다.** 눈대중 금지 — 요소의 `getBoundingClientRect()` 로 x/y/폭을 실측해 어긋남·삐뚤어짐을 확인.
- 안 보이는 글자(대비 부족)·중복 표시·사진 높이 어긋남까지 함께 점검.

### 5-4. 검증 후 보고
- **세영님께 "보세요"라고 하기 전에, 내가 먼저 실제로 렌더해서 확인한다.** (§6 방법)
- 완료·검증됐으면 담백하게 보고. 안 된 건 안 됐다고 말한다.

### 5-5. 검수 타이밍
- **다듬는 중에는 CSS를 고치면서 캡처로만 확인**, 전체 QA는 디자인 확정 후 1회.
- **검수(측정) 도는 중에 파일을 고치지 않는다** — 앞뒤가 다른 사이트를 잰 꼴이 됨(하루 세 번 겪은 실수).

### 5-6. 저작권
- **OpenStreetMap 지도**(contact.html 오시는 길): 저작자 표시(ODbL)가 **의무**. iframe 하단 링크 바가 지저분해 잘라냈지만, "지도 데이터 © OpenStreetMap 기여자" 캡션을 반드시 남긴다(현재 그렇게 되어 있음).
- **타사 로고·이미지**를 무단으로 가져와 쓰지 않는다. 로고 파일은 업체가 제공하면 교체(없으면 회사명 텍스트로 임시 표기).
- 프로젝트 실적 사진은 **업체 제공 실사진만** — AI 생성/임의 이미지 금지.

---

## 6. 검증 방법 (이 환경의 핵심 노하우)

이 PC(Windows)에서 사이트를 **눈으로 확인하는 법**:

- **브라우저 pane(preview) 스크린샷이 자주 타임아웃**됨("pane is not displayed"). 그럴 땐 **별도 포트로 headless 크롬을 띄우고 CDP(Chrome DevTools Protocol)로 직접 스크린샷**을 찍는다.
  - 크롬 실행 예: `chrome.exe --remote-debugging-port=9334 --user-data-dir=<임시경로> --headless=new --window-size=1440,1000`
  - PowerShell `System.Net.WebSockets.ClientWebSocket` 로 CDP 접속 → `Page.navigate` → `Page.captureScreenshot` → PNG 저장 → `Read` 로 확인.
  - 특정 섹션은 `getBoundingClientRect().top+scrollY` 로 위치를 구해 `window.scrollTo` 후 촬영.
  - ⚠️ 이 검증 스크립트들은 **세션 임시폴더(scratchpad)에 있어 세션이 끝나면 사라짐** → 다음 세션에서 다시 만들어야 함(위 절차대로).
- **정렬 실측**: CDP `Runtime.evaluate` 로 `getBoundingClientRect()` 를 JSON으로 받아 좌표 비교.
- **headless 크롬 좀비화 주의**: 같은 크롬 인스턴스를 여러 번 재사용하면 느려지거나 멈춤 → 새 `--user-data-dir`(매번 다른 경로)로 **clean 재시작**하면 해결.

---

## 7. 환경 주의사항 (함정)

- **PowerShell 5.1 은 .ps1 안의 한글 리터럴을 깨뜨린다**(BOM 없으면). → 스크립트에 한글을 직접 넣지 말고, 경로/문자열은 `[char]0xD648` 식 유니코드 코드포인트로 조립하거나, 파일을 **UTF-8 BOM** 으로 저장. 정규식에 한글 넣을 때도 동일.
- **Python 은 MS Store 스텁**이라 일부 안 됨. 텍스트 추출 등은 PowerShell로 처리.
- **이미지 처리**(리사이즈·크롭·로고 누끼)는 PowerShell 픽셀 루프가 아니라 **인라인 C#(`Add-Type`) + System.Drawing** 으로. 4:3 크롭 등은 `DrawImage` 로.
- **Word(.docx) 문서 읽기**: 압축 해제(`unzip word/document.xml`) 후 XML의 `<w:t>` 태그 텍스트를 PowerShell UTF-8로 추출(bash 콘솔은 한글이 깨져 보임). 이미지는 `word/media/image*.png` 를 `Read`.

---

## 8. 작업 이력 요약 (최근순, 자세한 건 `git log`)

> ### ⏸ 이어서 할 것 (2026-08-31 중단 시점)
> - 8/24 업체 수정사항 **전부 반영 완료 · 아직 커밋 안 함**(작업 트리에만 있음). 세영님 검수 후 커밋·푸시.
> - 남은 확인 사항: 라이프사이클 7번 `AFTER SERVICE` 대소문자, Commissioning Scope 블록 배치 위치, 울산 지사 전화·팩스 번호(미수령), 작은 파란 라벨의 명암비(사이트 전역 패턴이라 별건).
> - 수정 전/후 대조용: 원본 스냅샷 `Desktop\k1-homepage-수정전\`(= 커밋 `e5ca5d1`), 비교 이미지 `Desktop\k1-수정전후 비교\`. 둘 다 임시 폴더라 지워도 무방(`git archive HEAD`로 재생성).

- **8/24 업체 수정사항 반영** (2026-08-28): `케이원\수정\8월 24일 수정\(주)케이원 홈페이지 수정사항 정리본.docx` 지시 반영.
  - index: 히어로 아이브로우·설명문 교체, 듀얼도메인 보드 항목 교체(K1=해양플랜트·FPSO·육상플랜트 + Integrated Commissioning / OCEAN=Commercial·Special·LNG Carrier + E&I&M), 브랜드 영문 4문구, 사업분야 03(Instrumentation & Control)·06(친환경·탈탄소, 4항목), **KES 섹션 전면 교체**(12%·15% 등 성과 숫자 전부 삭제 → KES 실행축 5 + K1 Commissioning Lifecycle 7단계 + 타사와의 차별점 4), 프로젝트 리드에서 "75%" 삭제, 고객사(현대삼호중공업 제외 / SB선보·유진기공·동화뉴텍 추가 — **실제 로고 반영 완료**, 원본은 `주요고객사 로고/`).
  - about: CEO 인사말 전문 교체, 회사 연혁 리드·6지표 라벨 교체, 2018~2026 가로 타임라인 신설, Commissioning Scope 6항목 아이콘 블록 신설(**배치 위치 확인 필요**). 아이콘은 아이콘 레퍼런스 DB에서 가져와 `assets/icons/scope-*.svg`.
  - news: 게시판 리드 교체, **CI 섹션 신설**(`#ci` — 심볼 마크 설명 + 마스코트 키오/케이랑이). 이미지는 docx에서 추출 → `assets/ci/`. 전 페이지 메가·드로어·푸터에 CI 링크 추가.
  - contact: 문의 목적·회사 드롭다운 추가, 예시 placeholder 삭제, 영암 지사 TEL 061-460-3937 / FAX 061-462-7037.
  - 전 페이지: 대표 이메일 `k1ocean@` → `minseon.kim@k1tnc.co.kr`, 푸터 주소 라벨 본사/지사 → **K1**(거제 + 울산) / **K1 OCEAN**(영암).
  - 추가 작업(세영님 지시): 히어로 설명문 '하나의' 앞 줄바꿈 / 타사와의 차별점·Commissioning Scope 아이콘을 **아이콘 레퍼런스 DB**에서 가져와 교체(`assets/icons/diff-*.svg`, `scope-*.svg`) / KES 통합관리 문장을 차별점 리드로 이동 / CI·마스코트에서 카드 제거(케이랑이 3컷은 흰 배경 지워 컷아웃) / 고객사 목록 `projects.html`에도 동일 반영.
  - **문서 밖 일관성 정리 9건**(세영님 승인, 2026-08-31): projects "75%" 문장, business 계장 라벨·기술지원 제목, 메인 지표 라벨, 창립 9주년 표기, 푸터 소개문 7페이지+메타, 회사개요 리드·업종, 오시는 길 태그(K1/K1 OCEAN)·울산 인근 문구, mockup 3종 삭제.
- **OSM 지도 하단 바 크롭** (`bf40db9`): 오시는 길 지도 iframe 하단 링크 바를 잘라내고 attribution 캡션으로 대체.
- **index2/contact2 추가 후 취소** (`52521cc`→`19bbcd3` revert): 업체가 8/11 수정사항(.docx)을 줘서 `index2.html`·`contact2.html` 을 만들었으나 **업체가 취소 요청** → revert. **내용은 커밋 `52521cc` 에 보존**되어 있어 필요시 복구 가능.
- **프로젝트 실적 사진 6종** (`82e7d73`·`66e73d0` 등): P79 FPSO / WTIV CADELER / SHELL PRELUDE / CHEVRON / EXXONMOBIL / Woodside — projects.html + index.html 카드에 반영(4:3 1200×900).
- **브랜드 문구·값 문구 수정**: "끝까지 책임"→"확실한 책임", K1/K1 OCEAN 소개문 교체 등.
- (이전 세션) **포인트 컬러 오렌지→블루 전면 교체**, 신한금융→신한중공업 로고 교체, About 아이콘·정렬, 인증 아이콘, 모바일 헤더/다크모드 누끼 수정 등.

## 9. 남은 작업 / 미완 (오픈 전 처리)

- **고객사 로고 3종 미수령**: SB선보 · 유진기공 · 동화뉴텍 — 현재 `index.html` 로고월에 **회사명 텍스트(`<i class="txt">`)로 임시 표기**. 로고 파일 받으면 `assets/logos/`에 넣고 `<img>`로 교체.
- **8/24 문서 관련 확인 대기**(§8 참조): Commissioning Scope 블록 배치 위치, `business.html`의 "Instrumentation" 라벨·기술지원 섹션도 index와 맞출지, `projects.html`의 "75%" 문장, index Figures의 "대표이사 시운전 업력" 표기, 라이프사이클 7번 `AFTER SERVICE` 대소문자.
  - ※ 문서 [24] `INSTURMENTATION & CONTROL` 은 **문서 측 오타**(같은 문서의 캡처에는 `INSTRUMENTATION`으로 표기). 세영님 확인 후 `INSTRUMENTATION & CONTROL` 로 반영 완료.
  - ※ 원칙: 문서의 **철자는 그대로** 따르되, **대소문자는 주변 문구에 맞춤**(세영님 지시, 2026-08-31).
  - ※ 원칙: **수정사항 문서에 없는 부분은 손대지 않는다**(업체가 준 것이라 임의 변경 시 문제가 됨). 단 **주요 고객사 목록은 `projects.html`에도 동일 반영**하기로 결정(세영님 지시, 2026-08-31).
- `news.html` 갤러리·게시판: 실데이터 없어 빈 상태/placeholder → 실제 공지·뉴스·갤러리 데이터 오면 채우기.
- `recruit.html` 채용공고: 빈 상태 → 실제 공고 오면 채우기.
- 오픈 확정 시: OG 태그·`sitemap.xml`·`robots.txt`·애널리틱스·문의 폼 실제 전송 서비스(현재 `mailto:`) 연결.
- 실주소 정확도: 영암 지사 지도 좌표는 근사치(OSM에 번지 데이터 없음). 정확히 하려면 카카오맵 JS키 필요.

---

## 10. 관련 자료 위치

- 업체 수정요청 원본: `C:\Users\PC\Desktop\케이원\수정\` 하위 (.docx/.pptx)
- 로고 원본(.ai): `C:\Users\PC\Desktop\케이원 로고 수정\` 등 (벡터 PDF → WinRT `Windows.Data.Pdf` 로 PNG 렌더 후 사용)
- 백업: `_archive/`

---

*문의/의사결정 주체는 세영님(하오웹 실무). 애매한 건 넘겨짚지 말고 세영님께 질문할 것.*
